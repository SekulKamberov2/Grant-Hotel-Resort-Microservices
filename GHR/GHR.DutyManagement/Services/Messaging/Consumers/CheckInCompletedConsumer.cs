namespace GHR.DutyManagement.Services.Messaging.Consumers
{
    public class CheckInCompletedConsumer //: IConsumer<CheckInCompletedMessage>
    {
       // public async Task Consume(ConsumeContext<CheckInCompletedMessage> context)
       // {
          //  var userId = context.Message.UserId;
           // var checkInTime = context.Message.CheckInTime;

            // Apply logic to DutyService (e.g., assign shift, mark availability)
       // }
    }
}
